# PR 11A — Dark Marketing Foundation + Responsive Hero

- Branch/base: `feature/landing-redesign-hero` from `origin/main` at `bd6d113b127eb58f1acb89d6ab77124ea3ef5cb2`; the branch has no upstream.
- Objective: replace the landing Hero with the approved static cinematic composition, establish marketing-only dark tokens, preserve valid site navigation, and keep simulator/optics/rendering contracts unchanged.
- Changed surfaces: `LandingHero`, approved artwork wrapper, shared site header menu/focus behavior, home shell full-bleed class, marketing CSS foundation/theme, Hero copy, menu copy, and focused integration/E2E coverage.
- Approved Hero asset: `public/assets/landing/hero.png` (1672 × 941); loaded through `publicAssetUrl` with responsive `sizes="100vw"` markup. The legacy Hero artwork is not used.
- Responsive decisions: cinematic Hero is full-bleed at desktop/tablet/mobile; desktop keeps floating navigation, tablet uses the accessible menu at `<=1100px`, and mobile uses the simplified crop plus the catalog-oriented `Explore Scenes` / `瀏覽場景` CTA at `<=767px`. Headline remains two semantic lines with one shared typography rule.
- i18n: English canonical `home.hero` now exposes `eyebrow`, `titleLine1`, `titleLine2`, `description`, `startExploring`, and `exploreScenes`; zh-HK matches the shape. Added localized menu open/close labels under `common.nav`; locale resolver and persistence are unchanged.
- Validation: `npm test` (155 files, 1510 tests); `npm run typecheck`; `npm run lint`; `npm run check:css`; `npm run build`; `git diff --check`; focused Chromium landing suite (9 passed at desktop/tablet/mobile, including locale switch, CTA route, overflow, and menu focus behavior). Screenshots inspected at 1440, 1024, 768, and 390 widths.
- Not run: full `npm run ci:local:e2e` and renderer-wide E2E; this slice does not change simulator rendering, optics, RTT, lifecycle, or simulator routes. In-app Browser bootstrap was unavailable because its runtime import was blocked by the execution environment; Playwright supplied the browser evidence.
- Known gaps: legacy three InfoCards remain below the Hero as a temporary dark-themed boundary; no feature-strip redesign, Part E, Part F, final CTA, motion, parallax, or reveal effects are included.
